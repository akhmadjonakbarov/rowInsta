/* class CommentPlan {
  final String id;
  final String name;
  final double price;

  CommentPlan({required this.id, required this.name, required this.price});

  factory CommentPlan.fromJson(Map<String, dynamic> json) {
    return CommentPlan(
      id: json['id'],
      name: json['name'],
      price: json['price'].toDouble(),
    );
  }
} */

